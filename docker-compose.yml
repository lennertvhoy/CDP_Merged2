services:
  # ==========================================
  # PostgreSQL query plane for the chatbot
  # ==========================================
  postgres:
    image: postgis/postgis:15-3.4-alpine
    container_name: cdp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: cdpadmin
      POSTGRES_PASSWORD: cdpadmin123
      POSTGRES_DB: cdp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./schema_local.sql:/docker-entrypoint-initdb.d/01-schema.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U cdpadmin -d cdp" ]
      interval: 5s
      timeout: 5s
      retries: 5
    command: >
      postgres
      -c shared_buffers=512MB
      -c effective_cache_size=1536MB
      -c work_mem=16MB
      -c maintenance_work_mem=128MB
      -c max_connections=200
      -c checkpoint_completion_target=0.9
      -c wal_buffers=16MB
      -c default_statistics_target=100

  # ==========================================
  # Elasticsearch for Tracardi
  # ==========================================
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: cdp_merged_elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - es_data:/usr/share/elasticsearch/data
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:9200 || exit 1" ]
      interval: 10s
      timeout: 10s
      retries: 5

  # ==========================================
  # Redis for Tracardi
  # ==========================================
  redis:
    image: redis:alpine
    container_name: cdp_merged_redis
    restart: unless-stopped

  # ==========================================
  # MySQL for Tracardi
  # ==========================================
  mysql:
    image: mysql:8.0
    container_name: cdp_merged_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-tracardi}
      MYSQL_USER: ${MYSQL_USER:-tracardi}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-changeme}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  # ==========================================
  # Tracardi API
  # ==========================================
  tracardi-api:
    image: tracardi/tracardi-api:1.0.0
    container_name: cdp_merged_tracardi_api
    restart: unless-stopped
    ports:
      - "8686:80"
    environment:
      - ELASTIC_HOST=cdp_merged_elasticsearch
      - ELASTIC_PORT=9200
      - REDIS_HOST=cdp_merged_redis
      - MYSQL_HOST=cdp_merged_mysql
      - MYSQL_PORT=3306
      - MYSQL_USERNAME=${MYSQL_USER:-tracardi}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:-changeme}
      - MYSQL_DATABASE=${MYSQL_DATABASE:-tracardi}
      - LOGGING_LEVEL=info
      - INSTALLATION_TOKEN=${TRACARDI_INSTALLATION_TOKEN:-mytoken}
      - API_URL=${TRACARDI_PUBLIC_URL:-http://localhost:8686}
      - TRACKER_URL=${TRACARDI_PUBLIC_URL:-http://localhost:8686}
    depends_on:
      elasticsearch:
        condition: service_healthy
      redis:
        condition: service_started
      mysql:
        condition: service_started

  # ==========================================
  # Tracardi GUI
  # ==========================================
  tracardi-gui:
    image: tracardi/tracardi-gui:1.0.0
    container_name: cdp_merged_tracardi_gui
    restart: unless-stopped
    ports:
      - "8787:80"
    environment:
      - API_URL=${TRACARDI_PUBLIC_URL:-http://localhost:8686}
      - TRACKER_URL=${TRACARDI_PUBLIC_URL:-http://localhost:8686}
    depends_on:
      - tracardi-api

  # ==========================================
  # Wiremock for Flexmail simulation (optional)
  # ==========================================
  wiremock:
    image: wiremock/wiremock:latest
    container_name: cdp_merged_wiremock
    ports:
      - "8080:8080"
    command: [ "--verbose", "--global-response-templating" ]

  # ==========================================
  # DEPRECATED: Chainlit/Agent service removed
  # ==========================================
  # The Chainlit-based chatbot service has been deprecated.
  # All chat/UI functionality now goes through:
  #   - Operator Shell (Next.js on port 3000)
  #   - Operator API (FastAPI on port 8170)
  #
  # To start the new chat stack:
  #   1. Start Operator API: uvicorn src.operator_api:app --host 127.0.0.1 --port 8170
  #   2. Start Operator Shell: cd apps/operator-shell && npm run dev
  #
  # Date deprecated: 2026-03-14

volumes:
  postgres_data:
  es_data:
  mysql_data:
